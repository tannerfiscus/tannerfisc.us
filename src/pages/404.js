import React from 'react'
import Layout from '../components/Layout'

const NotFoundPage = () => (
  <Layout>
    <div>
      <h1>Page Not Found</h1>
      <p>I'm sorry to report that the page or resource you are looking for does not exist.</p>
      <p>It's possible the page was renamed, so let's start fresh by clicking one of the links above?</p>
    </div>
  </Layout>
)

export default NotFoundPage
